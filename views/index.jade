extends layout

block content
  h1= title
  p Welcome to #{title}

  form(name='search xxx', action='/search', method='get')
      div.input
          input(type='text', name='value')
      div.actions
          input(type='submit', value='Search')
